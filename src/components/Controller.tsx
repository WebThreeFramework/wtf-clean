import { WTComponent } from "types/common";
import { MTControllerArgs } from "types/components/controller";

const WTController: WTComponent<MTControllerArgs> = ({}) => {
    return (
        <>
            Test
        </>
    )
}

export default WTController;